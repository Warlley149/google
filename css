/*Reset Básico*/
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
  font-family: Arial, Helvetica, sans-serif;
  background: #ffffff;
  color: #222;
  min-height: 100vh;
}

header {
  position: absolute;
  top: 16px;
  right: 24px;
}

header ul{
  display: flex;
  list-style: none;
  gap: 16px;
}

header a {
  text-decoration: none;
  color: #202124;
  font-size: 14px;
}

header a:hover {
  text-decoration: underline;
}

.apps {
  position: relative;
  display: inline-block;
}

.apps summary {
  list-style: none;           
  cursor: pointer;
  border: none;
  background: transparent;
  display: inline-grid;
  place-items: center;
  width: 44px;
  height: 44px;
  border-radius: 50%;
  color: #5f6368;
  margin: 0;
}
.apps summary::-webkit-details-marker { display: none; } 

.apps summary:hover {
  background: rgba(60,64,67,0.06);
  color: #202124;
}

.apps-panel {
  position: absolute;
  right: 0;                     
  top: calc(100% + 10px);       
  width: 300px;
  background: #fff;
  border-radius: 12px;
  box-shadow: 0 8px 24px rgba(60,64,67,0.18);
  padding: 12px;
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 10px;
  transform-origin: top right;
  opacity: 0;
  visibility: hidden;
  transform: translateY(-6px) scale(.98);
  transition: opacity .18s ease, transform .18s ease, visibility .18s;
  z-index: 50;
}

.apps[open] .apps-panel {
  opacity: 1;
  visibility: visible;
  transform: translateY(0) scale(1);
}

.app-item {
  text-decoration: none;
  color: #202124;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8px;
  padding: 8px;
  border-radius: 8px;
  transition: background .12s, transform .08s;
}
.app-item:hover {
  background: rgba(60,64,67,0.06);
  transform: translateY(-2px);
}

.app-icon {
  width: 44px;
  height: 44px;
  border-radius: 10px;
  display: grid;
  place-items: center;
  font-size: 20px;
  background: #f7f7f7;
}

.app-name {
  font-size: 12px;
  color: #5f6368;
  text-align: center;
}

.apps-more {
  grid-column: 1 / -1; 
  margin-top: 6px;
  text-align: center;
}
.apps-more a {
  text-decoration: none;
  color: #1a73e8;
  font-size: 13px;
}

@media (max-width: 420px) {
  .apps-panel { width: 90vw; left: 5vw; right: 5vw; }
  .apps { width: auto; }
}

main {
  flex: 1;
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
}

body {
  min-height: 100vh;           
  display: flex;               
  flex-direction: column;      
  align-items: center;         
  justify-content: center;     
  font-family: 'Roboto', sans-serif; 
}

/* ======= Barra de pesquisa isolada (cole só este bloco) ======= */
.search-form {
  width: 100%;
  max-width: 640px;   /* largura máxima da barra */
  margin: 0 auto;     /* centraliza horizontalmente */
}

.search-wrap {
  display: flex !important;      /* força comportamento flex só aqui */
  flex-direction: row;           /* garante ordem normal */
  align-items: center;
  gap: 8px;
  padding: 10px 14px;
  border: 1px solid #d9d9d9;
  border-radius: 28px;
  background: #fff;
  width: 100%;
  box-sizing: border-box;
  box-shadow: 0 1px 6px rgba(0,0,0,0.06);
}

.search-input {
  flex: 1 1 auto;    /* ocupa o espaço restante sem quebrar layout */
  min-width: 0;      /* evita overflow dentro do flex */
  border: none;
  outline: none;
  font-size: 16px;
  padding: 8px 6px;
  background: transparent;
}

/* botões de ícone (apenas aparência — sem alterar posição do layout geral) */
.search-wrap .icon-btn {
  background: transparent;
  border: none;
  padding: 6px;
  border-radius: 8px;
  cursor: pointer;
  display: inline-flex;
  align-items: center;
  justify-content: center;
}

/* garantir visível a ordem esperada (teclado - microfone - lupa) */
.search-wrap .keyboard-btn { order: 0; }
.search-wrap .mic-btn { order: 1; }
.search-wrap .search-btn { order: 2; }

/* foco e hover (acessibilidade) */
.search-wrap .icon-btn:focus { outline: 2px solid #a7cdf5; outline-offset: 2px; }
.search-wrap .icon-btn:hover { background: rgba(0,0,0,0.04); }

/* botões abaixo — apenas se já existirem, evita sobrescrever regras globais */
.search-form > .buttons {
  margin-top: 12px;
  display: flex;
  gap: 10px;
  justify-content: center;
  width: 100%;
}
.search-form > .buttons button {
  background: #f2f2f2;
  border: 1px solid #dcdcdc;
  padding: 8px 14px;
  border-radius: 4px;
  cursor: pointer;
  font-size: 14px;
}

/* efeito hover simples */
.icon-btn:hover { background: rgba(0,0,0,0.04); }

/* botão Modo IA (destacado) */
.ia-btn {
  background: #1a73e8;
  color: white;
  border: none;
  padding: 6px 10px;
  border-radius: 16px;
  font-size: 13px;
  cursor: pointer;
}

/* botões abaixo da barra */
.buttons {
  margin-top: 12px;
  display: flex;
  gap: 10px;
  justify-content: center;
}
.buttons button {
  background: #f2f2f2;
  border: 1px solid #dcdcdc;
  padding: 8px 14px;
  border-radius: 4px;
  cursor: pointer;
  font-size: 14px;
}
.buttons button:hover { background: #e9e9e9; }

.logo {
    width: 272px;
    height: auto;
    margin-bottom: 24px;
}

form {
    display: flex;
    flex-direction: column;
    align-items: center;
}

.buttons {
    display: flex;
    gap: 8px;
}

button {
  padding: 10px 16px;
  border-radius: 4px;
  border: 1px solid #f8f9fa;
  cursor: pointer;
  background-color: #ffffff;
  font-size: 14px;
}

.search-box .search-btn {
    position: absolute;
    left: 15px;
    top: 50%;
    transform: translateY(-50%);
    background: none;
    border: none;
    font-size: 18px;
    cursor: pointer;
    color: #5f6368;
}

.search-box .search-btn:hover {
    color: #000;
}


footer {
    background-color: #f2f2f2;
    padding: 12px 60px;
    display: flex;
    justify-content: space-between; /* mantém os lados separados */
    align-items: center; /* centraliza verticalmente */
    font-size: 13px;
}

.footer-container {
    max-width: 960px;   /* largura máxima do conteúdo */
    margin: 0 auto;     /* centraliza na tela */
    display: flex;
    justify-content: space-between;
    align-items: center;
    padding: 10px 20px;
    font-size: 14px;
    color: #555;
}

/* Container para links à esquerda */
.footer-left {
    display: flex;
    gap: 15px;
    margin-left: 20px;
}

/* Container para links à direita */
.footer-right {
    display: flex;
    gap: 15px;
    margin-right: 20px;
}

.footer-left a, .footer-right a {
    margin: 0 10px; 
    color: #5f6368;
    text-decoration: none;
}

.footer-left a:hover, .footer-right a:hover {
    text-decoration: underline;
}
